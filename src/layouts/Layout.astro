---

---

<html>
  <head>
    <title>Junghoon Nam</title>

    <style>
      body {
        margin: 0;
        font-family: Arial, sans-serif;
        background-color: black;
        color: white;
      }

      /* 상단 바 */
      header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 60px 100px 20px 100px; /* 위쪽 더 내림 + 좌우 여백 */
      }

      /* 왼쪽 로고 */
      .logo {
        margin-left: 20px;
      }

      .logo a {
        text-decoration: none;
        color: white;
      }

      .name {
        font-size: 30px; /* 조금 줄임 */
        font-weight: bold;
      }

      .composer {
        font-size: 16px; /* 조금 줄임 */
        letter-spacing: 3px;
        margin-top: 3px;
      }

      /* 가운데 메뉴 */
      nav {
        display: flex;
        gap: 40px;
        font-size: 18px; /* 조금 줄임 */
      }

      nav a {
        text-decoration: none;
        color: white;
      }

      nav a:hover {
        opacity: 0.6;
      }

      /* 오른쪽 SNS */
      .sns {
        margin-right: 20px;
      }

      .sns img {
        width: 26px;
        height: 26px;
      }

      /* 가운데 이미지 */
      .center-image {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 75vh;
      }

      .center-image img {
        max-width: 800px; /* 🔥 더 크게 */
        width: 90%;
      }

      main {
        max-width: 900px;
        margin: 0 auto;
        padding: 60px;
        font-size: 20px;
        line-height: 1.8;
      }
    </style>
  </head>

  <body>

    <header>

      <!-- 왼쪽 -->
      <div class="logo">
        <a href="/">
          <div class="name">Junghoon Nam</div>
          <div class="composer">COMPOSER</div>
        </a>
      </div>

      <!-- 가운데 -->
      <nav>
        <a href="/">Home</a>
        <a href="/about">About</a>
        <a href="/works">Works</a>
        <a href="/events">Events</a>
        <a href="/archive">Archive</a>
        <a href="/contact">Contact</a>
      </nav>

      <!-- 오른쪽 -->
      <div class="sns">
        <a href="https://www.instagram.com/junghoon.n/" target="_blank">
          <img src="https://cdn-icons-png.flaticon.com/512/2111/2111463.png">
        </a>
      </div>

    </header>

    <div class="center-image">
      <img src="/mybackground.jpg">
    </div>

    <main>
      <slot />
    </main>

  </body>
</html>
